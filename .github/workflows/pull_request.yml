name: Merge
on: pull_request
jobs:
  on-create:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v3
      - name: "Move into repo"
        run: "ls"
      - name: "Print event"
        run: echo '${{ toJSON(github) }}'
